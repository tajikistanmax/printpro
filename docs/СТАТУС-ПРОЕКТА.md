# СТАТУС ПРОЕКТА PrintPro

> Живой документ «где мы остановились». Обновляется в конце каждой сессии.
> Читается первым при возобновлении работы (в т.ч. в новом чате / на другом компьютере).

**Последнее обновление:** 2026-07-08 (вечер — аудит + исправления, НЕ закоммичено)
**Последний коммит:** `0e135d5` — runtime-companyId. ✅ **Всё запушено** (21 коммит сессии, `main` = `origin/main`). **Облако ЗАДЕПЛОЕНО и работает** (CORS-фикс live-verified).

## 🔧 Сессия 2026-07-08 (вечер) — Полный аудит + исправления (в рабочем дереве, не закоммичено)
Полный отчёт аудита: [`docs/АУДИТ-2026-07-08-полный.md`](АУДИТ-2026-07-08-полный.md). Проведён живой запуск (API+Web+БД), HTTP-E2E, визуальный тест (headless-скриншоты залогиненных страниц), статический аудит 4 агентами (безопасность/деньги/фронт/архитектура). Сборка API (tsc+nest build) и Web (tsc) — зелёная; ключевые фиксы live-проверены.

**Исправлено (17 находок):**
- **Безопасность:** (1) `/settings/ui` больше не отдаёт банковские реквизиты анониму — `payTransferQr/Requisite` вынесены в авторизованный `/settings/receipt-info` (POS обновлён), live-verified; (2) `/system/company-id` отдаёт id только при 1 компании в БД (в мультитенанте — `null`), live-verified; (3) загрузка файла клиента захарднена (`DOCUMENT_UPLOAD_OPTIONS`: whitelist MIME/ext, без HTML/SVG, `UPLOADS_DIR`); (4) public online-заказ проверяет принадлежность `serviceId`; (5) `users.create` проверяет `branchId`; (6) `JWT_SECRET` обязателен везде кроме `test`.
- **Деньги:** (7) **С14 гонка кредит-лимита закрыта** — `SELECT … FOR UPDATE` на клиенте (была помечена «закрыта» ошибочно); (8) возврат бонусов — атомарный `GREATEST(0, bonusPoints - earned)` вместо read-modify-write (refund + createReturn); (9) **`addPayment` идемпотентен** — миграция `Payment.idempotencyKey`, повтор не двоит оплату, live-verified (paid=40 при повторе, 60 при новом ключе); (10) `shiftId` в оплате проверяется с `userId` (не в чужую смену); (11) импорт CSV отбраковывает отрицательные цены.
- **С7 (операции без смены):** **фактически закрыт в коде** (подтверждено аудитом) — `addPaymentTx` требует открытую смену; все нал-расходы через `openShiftId()`.
- **БД:** (12) миграция `payment_idempotency_and_hot_indexes` — индексы на `OrderItem`, `StockReceiptItem`, `QuoteItem`, `ClientDebt`, `ServicePriceTier/Size/Option`, `AuditLog` (49 миграций, применена).
- **Инфра:** (13) добавлен **CI** (`.github/workflows/ci.yml` — tsc+build+test+lint для api и web); (14) web-Dockerfile больше не зашивает `NEXT_PUBLIC_API_BASE=localhost` (LAN-автоопределение касс восстановлено); (15) api-Dockerfile добавляет идемпотентный seed (свежий узел получает `admin`); (16) `db-backup.yml` аккуратно пропускается без секретов (нет «красных» прогонов).
- **Фронт:** (17) убраны проглоченные `catch {}` (services delete / design checklist с откатом / warehouse); POS-кнопка «Оплатить» блокируется на время запроса (ref-guard + `disabled`); убран вводящий в заблуждение хардкод версии на логине.
- **Чистка:** `.gitignore` для `.pyc`/`__pycache__`/`settings.local.json`; `.pyc` и `settings.local.json` убраны из индекса git.

**⚠️ НЕ закоммичено** — правки в рабочем дереве. Прямой push в `main` заблокирован политикой; нужно явное «закоммить/запушь».
**Остаётся (решение владельца/отдельная работа):** sync tenant-скоуп + HMAC на shipping-воркере (`sync-worker.mjs`) — зависит от топологии облака; фронт-архитектура (общий data-хук, типизация 366 `any`, монолиты >1500 строк) — квалити-долг, не баг; `render.yaml` free→starter (биллинг); `embedded-postgres` beta; `asar:false`; вынос дизайн-скиллов `.kiro`/`.claude/skills` из git.

---
**Живой прод (проверено 2026-07-08):** `/api/health`→200, `/api/health/ready`→`{db:up}`, CORS с Origin→200 (был 500), `/api/system/company-id`→200, helmet + `x-request-id` на месте, публичный эндпоинт (БД+tenant)→200. Миграции применились автоматически.
**Коробка (Electron):** скелет отревьюен, **runtime-companyId сделан**, **✅ УСТАНОВЩИК СОБРАН** (`PrintPro Setup 0.1.0.exe`, ~283 МБ, +деинсталлятор, +`.blockmap`) **И ПРИЛОЖЕНИЕ РЕАЛЬНО ЗАПУСКАЕТСЯ** (2026-07-08). Установили .exe → нашли и исправили **3 рантайм-бага** (сборка их не ловит, только фактический запуск): (1) `ERR_REQUIRE_ESM` — `embedded-postgres` v17 это ESM, `require()` его роняет → грузим `import()`; (2) инициализация БД зависала — `child_process.spawn()` не редиректит asar-путь `initdb.exe` → **отключён asar (`"asar": false`)** + `withTimeout`-guard; (3) БД была WIN1251 (локаль системы) → миграция с «→» падала → задан **`initdbFlags: --encoding=UTF8 --locale=C`**. После фиксов полный конвейер при `role=main` проходит: Postgres → **48 миграций** → seed (компания DushanbePrint, роли, `admin`/`admin123`) → NestJS API стартует → Next.js web. **⚠️ Порты 3000/3001/5433 должны быть свободны** (на машине разработчика их занимает metrodushanbe). **✅ Живой прогон проверен end-to-end:** вход `admin`/`admin123` → JWT → `/api/auth/me` с 26 правами (packaged API + БД + auth). **Доп. харднинг коробки:** (а) **порт-защита** — при занятом порте понятная ошибка вместо тихого полу-запуска (проверка на всех интерфейсах); (б) **бэкапы** — холодная копия `pgdata` при старте + ротация (7 копий), т.к. `pg_dump` не в комплекте; (в) **логотип PrintPro** (`electron/build/icon.ico`). Осталось (решение владельца): авто-обновление (GitHub Releases vs свой сервер), подпись установщика, пилот 1–2 клиента. Сборочный блокер (GitHub CDN для `nsis-resources`) решён зеркалом **npmmirror** (`ELECTRON_BUILDER_BINARIES_MIRROR`). Детали всех фиксов — в `electron/README.md` («Проверено реальным ЗАПУСКОМ»). `electron/release/` в `.gitignore`.

---

## 🎯 ВИДЕНИЕ: коробочная версия (Electron) — читать перед стартом этой темы
Полное видение и план: [`docs/06-АРХИТЕКТУРА-КОРОБКА-ЭЛЕКТРОН.md`](06-АРХИТЕКТУРА-КОРОБКА-ЭЛЕКТРОН.md).
Коротко: клиент **скачивает программу → ставит одним кликом → работает**; **база встроена**
и разворачивается сама (клиент ничего не устанавливает); работа **офлайн/онлайн**;
внутри офиса — **локальная сеть** (главный ПК + кассы); сверху — **облако** (руководитель,
филиалы); **авто-обновление**. Принцип: **один код** — правим в одном месте, меняется и в
онлайне, и в коробке. Порядок фаз: 0) облако → 1) скелет Electron + встроенная база →
2) роли «главный/касса» + локалка → 3) облачная синхр. → 4) авто-обновление + `.exe` + лицензия →
5) нативный мобильный (пока PWA). **Статус: согласовано с владельцем, к реализации.**

---

## 🚀 Сессия 2026-07-08 — Продакшн-рывок: инфра-хардненинг + multi-tenant security + скелет коробки

**Цель владельца:** довести до продакшна и облако, и коробку, «максимально» (добивка + харднинг), с бригадой агентов. Оркестратор — Opus 4.8; finder/worker-агенты — Sonnet 5; верификация/синтез — Opus.

**Ключевое открытие (бригада из 10 агентов, live-проверка):** платформа УЖЕ задеплоена на Render (`printpro-api/web.onrender.com`), но **панель не работала из браузера** — `CORS_ORIGINS` не задан → API отдавал 500 на все браузерные запросы. Общая прод-готовность по аудиту была **30/100** (бизнес-логика вылизана — регрессий 82/100, но вся инфра имела блокеры).

**Сделано и закоммичено (9 коммитов `f56e789`..`6499bae`, всё build+tests зелёное, ключевое live-verified):**
- **Baseline починен** — рабочее дерево не компилировалось; собрано, 3 «висевшие» пачки правок закоммичены (CORS-хардненинг, устойчивость web, ценники).
- **Multi-tenant: alias-штрихкоды** (`544745b`) — глобальный `@unique` → company-scope + soft-delete (миграция `barcode_alias_company_scope`, live-verified).
- **API прод-хардненинг** (`337921e`) — `/api/health`(+`/ready` c SELECT 1), helmet, trust proxy, graceful shutdown (Prisma `$disconnect`), **глобальный ThrottlerGuard был написан, но не подключён — включил** (лимит 600/мин), env fail-fast, `UPLOADS_DIR`, `ALLOW_LAN_ORIGINS`. Live-verified (curl).
- **Деплой-конфиг** (`df63184`) — `render.yaml`: `CORS_ORIGINS` (снимает 500), `NODE_ENV`, healthCheckPath `/api/health`, region frankfurt; seed стал **идемпотентным** (не затирает кастомные права ролей) и **компилируемым** (`node dist/bootstrap/seed.js`, нужно и коробке). Live-verified.
- **Индексы** (`7e68828`) — `(companyId, createdAt)` на Order/StockMovement (горячий путь отчётов).
- **Cross-tenant IDOR закрыты** (`0fd516a`) — бригада (9 finder + adversarial verify) нашла и верифицировала: FK из тела запроса без проверки companyId в **orders** (clientId/branchId/serviceId/productId/staff), **quotes**, **purchasing** (productId), **payroll** (userId + утечка ФИО в Z-отчёт), **tasks** (orderId/assignedUserId). Паттерн эталона complaints.service.
- **Скелет коробки (Electron)** (`6499bae`) — пакет `electron/` (main-процесс, embedded-postgres, роль ПК, авто-обновление, установщик, бэкап). См. `electron/README.md`.

**⚠️ НУЖНО ОТ ВЛАДЕЛЬЦА:**
1. **Запушить** — 9 коммитов лежат локально (прямой push в `main` заблокирован политикой). После пуша Render передеплоит с CORS-фиксом. Быстрый ручной обход: в дашборде Render → `printpro-api` → Environment → `CORS_ORIGINS=https://printpro-web.onrender.com` → Save.
2. **План БД** free→starter (free-Postgres удаляется ~через 30 дней, без бэкапов = потеря данных).
3. **Файлы клиентов** (макеты/фото) — эфемерная ФС Render, теряются при рестарте. Нужно R2/S3/B2 или платный диск.
4. **Домен** — привязать HTTPS + добавить в `CORS_ORIGINS`.
5. **2 решения по логике:** (а) кредит-лимит клиента — по сумме до/после скидки quick-sale? (б) публичный онлайн-заказ — префикс `ORD` жёстко или из настройки `orderPrefix`?
6. **sync pull/push** не фильтруют по companyId — **межтенантная утечка** ЕСЛИ облако мульти-тенант в одной БД. Архитектурное: нужно решение по топологии деплоя (общая БД vs изолированный деплой на клиента), прежде чем писать патч (маппинг узел→компания + денормализация companyId в дочерние модели).

**Волна 3 — сделано (`0d981ff`, `c0d9524`, `e1474f3`, `bdb2a8f`, `c8601fb`):** глобальный фильтр ошибок + request-id (5xx больше не исчезают без следа); Sentry «под ключ» (активируется `SENTRY_DSN`, иначе no-op); фикс race `beforeQty/afterQty` в `production.service` (writeOffMaterials + reverseMaterialWriteOff); security-заголовки в `next.config` (HSTS/X-Frame/nosniff/Referrer/Permissions); экраны ошибок фронта (`error/global-error/not-found.tsx` — нет «белого экрана»); offsite-бэкапы БД (GitHub Actions pg_dump → R2/S3, `docs/07-бэкапы.md`); **идемпотентность stock-мутаций** (миграция `stock_idempotency_key`, receive/adjust/transfer/writeOff — завершает все 3 needs-schema-migration из добивки, live-verified).

**Осталось (не блокеры немедленного запуска):** структурные JSON-логи (nestjs-pino); httpOnly-cookie вместо localStorage (риск — трогает auth-flow, лучше с проверкой владельца); рантайм-резолв companyId на фронте (для тиражирования коробки на разных клиентов); Ed25519-лицензия коробки (Фаза 4); **sync tenant-скоупинг** (после решения владельца по топологии); фронт POS может начать слать `idempotencyKey` в stock-операциях (бэкенд уже принимает).

Полный приоритизированный план (17 блокеров + cloud/box/hardening) — в результатах gap-analysis этой сессии.

---

## 🚦 Сессия 2026-07-05 (продолжение) — Prod-readiness аудит: 25 P0 исправлено

Полный отчёт: [`docs/PROD-READINESS-2026-07-05.md`](PROD-READINESS-2026-07-05.md) (136 находок; 25 critical/high подтверждено кодом, 0 ложных; 110 medium/low — не верифицировались поштучно).

**Сквозной аудит всей платформы** 37 агентами (19 ревью-доменов → верификация критичных → синтез). Все **25 подтверждённых critical/high исправлены** тематическими коммитами `81130bb`..`f5f80cf` (каждый tsc/build-зелёный; денежные — с live-проверкой):
- **Деньги/возвраты (orders):** P0-1 скидка не писалась в строки → возврат считаем от нетто-цен (`netRatio`); P0-2 миграция `return_cash_refunded` — двойная выдача налички закрыта; P0-3 `addPayment` учитывает `returnedTotal`.
- **Отчёты:** P0-4 cashflow net − возвраты; P0-24 payables без срока → не просрочен; P0-25 маржа на item-based базе.
- **Каталог/закупки:** P0-5 импорт не обнуляет цены; P0-6 trim штрихкода; P0-7 tenant-проверка приёмки.
- **Производство:** P0-8 списание+COMPLETED в одной tx; P0-9 не откатывает терминальный статус заказа.
- **Зарплата/клиенты:** P0-10 атомарная выплата (нет двойной); P0-11 итоги по всем заказам (кредит-лимит).
- **Public/КП:** P0-12 `/public/services` требует companyId; P0-15 атомарная конвертация КП (маркер).
- **Sync:** P0-13/14 курсор не теряет данные (guard failed + overlap-окно −5 мин).
- **Схема:** P0-16 индексы (миграция `hot_indexes`).
- **Веб:** P0-17 бонус по реальному балансу; P0-18 сброс устаревшего промо; P0-19 экспорт всего набора; P0-20 guard двойного клика приёмки; P0-21/22/23 показ проглоченных ошибок.

**Миграции этой части:** `return_cash_refunded`, `hot_indexes`. **Сборка:** API tsc+nest build+тесты+prisma validate = PASS; Web tsc = PASS, lint 0 errors.

**Осталось (не в этом заходе):** 110 medium/low из отчёта (перепроверить поштучно перед фиксом), 1 спорная (payroll прораторация MONTHLY — решение владельца).

---

## 🛡️ Сессия 2026-07-05 — Deep-research аудит: верификация + добивание остатка

Полный отчёт находок: [`docs/deep-research-report.md`](deep-research-report.md).
Консолидированный результат: [`docs/АУДИТ-РЕЗУЛЬТАТ-2026-07-05.md`](АУДИТ-РЕЗУЛЬТАТ-2026-07-05.md).

**Что было:** прошлый прогон агентов применил правки на ~все модули (P0/P1/P2), но не проверил и не закоммитил (~4300 строк «висели»). В этой сессии 16 модульных агентов + QA **проверили каждую находку против кода**, закрыли пробелы, довели до зелёной сборки.

**Итог по 44 подпунктам:** 23 already-fixed (проверено), 12 fixed-now, 4 partial, 1 not-fixed (TODO), 1 needs-migration, 3 needs-owner-decision. Коммит `ef44aec` (63 файла). Схема БД в нём не менялась.

**Добито в этой же сессии (отдельный коммит, с миграцией и live-проверкой):**
- **P0-5 (sync replay):** подпись HMAC привязана к SHA-256 тела запроса + одноразовый `x-sync-nonce` (окно 5 мин, in-memory store). Клиент (`callCloud`) и сервер (`check`) согласованы. Live: `sync/push` без подписи → 403. ⚠️ Обновлять облако и коробку одновременно (протокол node-secrets ужесточён; legacy shared-secret не тронут). Для multi-instance деплоя nonce-store нужен общий (Redis).
- **P1-8 (отзыв токенов):** миграция `User.tokenVersion` (аддитивно, `DEFAULT 0`); версия кладётся в JWT (login + PIN), guard сверяет её с БД (токены без версии = 0 → обратная совместимость, деплой не разлогинивает всех); `resetPassword` инкрементит версию. Live: старый токен после bump → 401, повторный вход → 200. Осталось владельцу: короткий TTL привилегий (config), «выйти на всех устройствах» как явный эндпоинт.
- **P2-5 (rate-limit):** `@nestjs/throttler` — базовый лимит 60/мин + строгие `@Throttle` на публичных (`password-reset-request` 5/мин, `orders`/`reorder` 10/мин, `upload`/`my-orders` 20/мин). Live: 6-й запрос сброса пароля → 429. Осталось: OTP/CAPTCHA (нужна инфраструктура доставки), общий store для multi-instance.
- **P1-2 (финснапшот списаний):** миграция `production_writeoff_cost_snapshot` — `StockMovement.unitCost/totalCost` (Decimal 12,4) + `productionJobId` (FK, `ON DELETE SET NULL`). `writeOffMaterials` пишет себестоимость из `Product.purchasePrice` и связь с заданием → себестоимость выполненного заказа восстановима. Live E2E: завершение задания → `WRITE_OFF` c unitCost=10/totalCost=30/qty=3, фикстур убран, БД восстановлена. Коммит `29cc14d`.

**Проверено:** API `tsc`+`nest build`+тесты+`prisma validate` = PASS; сервер стартует; live-проверки P0-5/P1-8/P2-5 пройдены. Web не трогался в этой части (правки P2-6..P2-9 — в `ef44aec`).

- **P1-9e (логирование security-событий):** глобальный `SecurityAuditFilter` пишет в аудит guard-level 401/403, которые не доходят до интерсептора: все 403 (permission-denied) и 401 только при предъявленном токене (отклонён невалидный/отозванный — флуд анонимных сканов не пишется). Live: 401-с-токеном → `security:token-rejected`, 401-без-токена → нет записи, 403 → `security:forbidden` с ip/path/UA. Штатные ответы сохранены.
- **P1-9d (транзакционный аудит before/after):** новый `AuditService.recordTx(tx, {before, after})` пишет аудит ВНУТРИ той же транзакции; ошибку не глушит (падение аудита откатывает бизнес-операцию). 3 фазы, каждая с live-проверкой:
  - **A (RBAC, `8fb79a7`):** `rbac:role-permissions` (before/after коды), `role-create`, `user-create/active/password-reset/pin`; actor проброшен из контроллеров.
  - **B (деньги, `65c5c24`):** `money:shift-open/close`, `cash-movement`, `payment` (before/after paid/balanceDue/статус), `refund`, `return`, `quick-sale`, `supplier-payment`, `receipt`, `payroll-payout`.
  - **C (склад, `65178fc`):** `stock:receive/adjust/transfer/recount/recount-bulk/write-off`, `production-writeoff`, `stock:receipt`. Складские движения внутри orders не дублируются (уже в `StockMovement` + денежных событиях).

- **P0-7 (идемпотентность возвратов):** миграция `return_idempotency_key` — `Return.idempotencyKey String? @unique`. `createReturn` принимает ключ: повтор (двойной клик/ретрай POS) возвращает существующий документ (пред-проверка + catch P2002 на гонке), без задвоения возврата. Live: тот же ключ → тот же документ, `returnedTotal` не сдвинулся.

- **P0-5 durable nonce:** in-memory nonce-store заменён на таблицу `SyncNonce` (миграция `sync_nonce`, unique `(node, nonce)`, index `expiresAt`). Replay-защита sync теперь переживает рестарт процесса и работает при нескольких инстансах; просроченные nonce чистятся опортунистически. Live (node-secrets режим): валидный подписанный запрос с новым nonce → 201, повтор того же nonce → 403 «Sync nonce replay», запись сохранена в БД.

**Остаток аудита (бэклог, приоритет):** P0-5 ротация секретов (процедура dual-secret overlap), P2-5 OTP/CAPTCHA на публичных (нужен канал доставки), P2-10 глобальный 403-UX (политика), P1-9d-orders-stock (опц.). Фронт POS может начать слать `idempotencyKey` в возвратах (бэкенд уже принимает). Детали — в `АУДИТ-РЕЗУЛЬТАТ-2026-07-05.md`.

**⚠️ Не запушено:** 3 коммита (`ef44aec`, `aa8f366`, `29cc14d`) лежат локально, `main` впереди `origin/main`. Push прямой в `main` заблокирован политикой — нужно явное «запушь». При деплое P0-5: обновлять облако и коробку одновременно.

---

## 🔧 Сессия 2026-07-02 (вечер) — Аудит: исправления + второй экран + вход/склад

Полный аудит бизнес-логики: [`docs/04-АУДИТ-2026-07-02.md`](04-АУДИТ-2026-07-02.md).

**Закрыто (код исправлен и собран):**
- **Критичные К1–К5:** оплата «в долг» без денег; кросс-компанийный IDOR (companyId только из JWT); quickSale в одной транзакции с откатом бонусов/промокода; повтор оплаты после ошибки не возвращает отменённый заказ; возврат сторнирует бонусы.
- **Высокие В1–В7:** двойной/частичный возврат по остатку; гонки → атомарный `updateMany`; возврат по методу оплаты (нал OUT только для CASH-части); запрет CANCELLED через updateStatus; расхождение сумм фронт↔бэк; rate-limit логина + whitelist MIME загрузок.
- **Средние:** С1 (нормализация телефона), С5 (POS-фильтр показывает подкатегории), С6 (PATCH товара не затирает категорию/ед.), С8 (отложенный чек хранит скидку/промокод/бонус/клиента/примечание), С9 (второй экран не «залипает»: метка времени + сброс при закрытии кассы), С10 (ручная скидка зажата стоимостью товаров), С17 (тест-уведомления — companyId из токена).
- **Низкие:** удалённый клиент скрыт по прямой ссылке; `/stock/adjust` отклоняет приходные типы; кнопка «Чек» в заказе (`/r/:id`).
- **UX:** три оформления второго экрана («Витрина», «Каталог», «Промо») доведены до макетов; вход по PIN — ввод с физической клавиатуры; склад «Остатки» — пагинация (10/25/50/100).

**Миграция выполнена (`20260702152511_partial_unique_barcode_and_open_shift`, применена `migrate deploy`):**
- **С4** — частичный unique-индекс: один активный штрихкод на компанию (NULL/пустые и мягко удалённые не мешают). Код ловит P2002 → понятная ошибка.
- **С13** — частичный unique-индекс: одна открытая смена на кассира в компании (двойное открытие невозможно). `openShift` ловит P2002.
- **С3** (без миграции, логика) — оплата долга поставщику теперь гасит его приёмки (старые первыми): обновляет `paidAmount`/`paymentStatus`, снимает срок при полной оплате.
- **С2** не требуется: заявки на закупку — печатные снимки, конверсии «заявка→приёмка» в коде нет.
- Проверено: дубликатов в БД не было; тест подтвердил, что индекс отклоняет дубликат штрихкода (P2002).

**Ещё закрыто (сессия «вечер-2», код собран):**
- **С11** — удаление категории в транзакции: открепляем товары, подкатегории поднимаем на верхний уровень, мягкое удаление (deletedAt) вместо hard delete, проверка владельца.
- **С14** — кредитный лимит теперь проверяется внутри транзакции (гонка параллельных заказов закрыта).
- **С12** — массовая инвентаризация: таймаут транзакции поднят до 2 мин (300+ позиций не упрутся в дефолтные 5с).
- **Промокод** — `consume` атомарно проверяет `isActive`/`validUntil`/лимит (не только лимит).
- **Z-отчёт** — платежи и движения фильтруются по `deletedAt` (мягко удалённые не искажают отчёт).

**Осталось (требует решения владельца — бизнес-политика/семантика):**
- **С18** — DEBT-маркер платежа и `paymentStatus` при частичном погашении (влияет на строку «в долг» Z-отчёта задним числом).
- **С7** — запрещать ли денежные операции без открытой смены (сейчас проходят с `shiftId=null`, не попадают в Z-отчёт).
- **С16** — публичные эндпоинты раскрывают реквизиты/историю по телефону (что именно закрыть — решить).
- **Лимит-% скидки с правами** — нужно поле в настройках компании (миграция) + проверка права кассира.
- Прочие низкие: EAN-13 контроль при ручном вводе (может отклонять внутренние коды), `DocumentCounter` без года (миграция), JWT/CORS-хардненинг (config), страница «Долги» показывает и заказы «в работе» (возможно, так и задумано).

---

## 💵 Приёмка: «Оплата из кассы» (галочка)
Приёмка с оплатой сейчас теперь спрашивает источник денег: `StockReceipt.paidFromCash` (миграция `receipt_paid_from_cash`, дефолт true). Галочка «Оплата из кассы» в форме приёмки (Закупки):
- **вкл** (по умолч.) — расход по открытой смене (в Z-отчёте), как раньше;
- **выкл** — поставщик оплачен (долг уменьшается), но кассовый ящик/Z-отчёт не трогаются, открытая смена не требуется.
Проверено live: false → движения по кассе нет; true → движение OUT создано. То же правило можно распространить на оплату долга поставщику (`paySupplierDebt`), если понадобится.

## Что это за проект
Платформа для **типографии/полиграфии** (не продуктовый магазин — товар по весу не нужен).
Моно-репозиторий:
- `printpro-api` — бэкенд NestJS + Prisma + PostgreSQL
- `printpro-web` — фронтенд Next.js

Продаётся в двух моделях: облако (подписка) и коробка (установка на ПК).

## Как запустить локально
1. `printpro-api/.env` (не в git): `DATABASE_URL="postgresql://postgres:postgres@localhost:5432/printpro?schema=public"`, `JWT_SECRET="..."`, `PORT=3000`
2. `cd printpro-api && npx prisma migrate deploy && npx prisma generate && npx ts-node prisma/seed.ts`
3. API: `npm run start:dev` → http://localhost:3000/api
4. Веб: `cd printpro-web && npm run dev` → http://localhost:3001
5. Вход: `admin` / `admin123`. Касса — вход по PIN (задаётся в «Сотрудниках»).

---

## 📊 Сессия 2026-07-02 — Модуль отчётов (переработан и расширен)
Полноценный модуль отчётов и финансов. ТЗ: [`docs/03-Отчёты-модуль-ТЗ.md`](03-Отчёты-модуль-ТЗ.md) (назначение, роли, глоссарий метрик с формулами, каталог всех 20 отчётов, бизнес-процессы, справочник API, развитие). **Миграции БД не потребовались** — всё на существующей схеме.

**Бэкенд** (`printpro-api/src/reports/`): было 8 эндпоинтов → стало 20. Все под `reports.view`, единые фильтры `companyId/from/to/branchId`, где нужно `groupBy(day|week|month)`.
- Существующие 8 дополнены (старые поля сохранены — обратная совместимость): `summary` (+филиал, +сравнение с пред. периодом и дельты, валовая прибыль/маржа, новые клиенты, инкассация, прирост долга, доля без себест-ти), `sales-by-item` (+филиал/тип/категория, ключ по id, себест-ть/маржа/доля, флаг zeroCost), `profit`/`expenses`/`equipment-load`/`staff` (+филиал; staff +«получено деньгами»). `daily` и `debts` не менялись.
- Добавлены 12: `timeseries`, `sales-by-category` (двухуровневая), `sales-by-client`, `abc`, `cashflow`, `receivables` (aging), `payables`, `purchasing`, `inventory` (оценка склада), `stock-movements` (+списания), `production` (брак/срок/цикл), `orders` (реестр с фильтрами для выгрузки).

**Фронтенд** (`printpro-web/src/app/(panel)/reports/`): одна страница → хаб с 10 вкладками (Обзор, Продажи, Услуги/товары, Прибыль, Финансы, Долги, Склад, Закупки, Производство, Сотрудники). Общая панель фильтров: пресеты периода + произвольный диапазон, филиал, гранулярность, сравнение периодов. Экспорт **CSV** (`;`+BOM для Excel) в каждой таблице, печать/PDF (`window.print`). Файлы: `page.tsx` (хаб), `lib.ts` (хелперы/типы/экспорт), `sections.tsx` (секции вкладок). Новых зависимостей нет; графики — столбики на div (как раньше).

**Проверено:** API `tsc --noEmit` + `nest build` = 0; WEB `tsc --noEmit` + `eslint` (файлы отчётов) = 0. Полный `next build` не гонялся — блокировку `.next/dev/lock` держит запущенный dev-сервер. Ревью нашло 6 некритичных дефектов — исправлены (просрочка долга только при заданном сроке; оценка склада включает неактивные товары; задачи сотрудника считаются за период; локальные даты в бакетах вместо UTC).

**⚠️ Важно:** агент-сборщик по ошибке создал посторонние правки (новый скин кассы «Премиум», скин второго экрана «Люкс», смена дефолтного layout кассы) — **всё откатано**, в дереве только файлы отчётов.

**Осталось по отчётам:** закоммитить; на будущее (см. §10 ТЗ) — реальный XLSX, графики-библиотека, НДС/налоги, план-факт, отчёты по расписанию на e-mail/Telegram, снапшоты закрытых периодов, сравнение филиалов.

---

## 🐛 QA-сессия 2026-07-01 (деньги/приёмка/долги) — исправлено
Полный прогон приёмки и оплат на живой БД. Исправлены и проверены баги учёта:
1. **Оплаты поставщикам не отражались в кассе.** `paySupplierDebt` и оплата при приёмке (`createReceipt`) создавали `CashMovement` без `shiftId` (или не создавали вовсе) → расход не попадал в Z-отчёт, завышал наличные. Теперь расход привязан к открытой смене кассира.
2. **Возвраты (`refund`, `createReturn`) мимо кассы** — та же причина, привязаны к смене.
3. **Переплата заказа** не блокировалась (можно было внести больше суммы) → фантомная выручка и бонусы. Добавлена проверка «оплата ≤ остатку».
4. **Продажа «в долг» помечалась UNPAID**, а не `DEBT`. Теперь `quickSale` с `method=DEBT` ставит `paymentStatus=DEBT`.
5. **Строка «в долг» в Z-отчёте была всегда 0.** Долговая продажа теперь создаёт запись `Payment(method=DEBT)` (сумма=итог, привязана к смене), не меняя `paid/balanceDue`. Строка «в долг» в кассе и отчёте выручки заполняется; реальные деньги (`collected`, `expectedCash`) её исключают — не задваивается. Проверено: долг +250, выручка/наличные без изменений.

**UX-доработки этой сессии:**
- Новая страница **«Долги»** (`/debts`, меню → Продажи): долги клиентов (нам должны) + поставщиков (мы должны) в одном месте, оплата модалкой прямо оттуда.
- **«Сотрудники»**: «Новый сотрудник» — теперь кнопка + модальное окно (было открытой формой).
- **«Закупки»**: оплата долга поставщику — модальное окно вместо `window.prompt`.
- **«Склад»→«Приём»**: заметная плашка-ссылка на «Закупки» (несколько позиций + поставщик + оплата/долг).

**Сроки долгов + приёмка (доработано):**
- **Миграция** `order_debt_due_date`: у заказа поле `debtDueDate`. POS «в долг» принимает срок; на `/debts` — колонка «Срок», подсветка просрочки, кнопка задать срок (`PATCH /orders/:id/debt-due`). Напоминание «Просроченные долги» в колокольчике (ссылка на `/debts`).
- **Приёмка сведена к одной:** простая приёмка со «Склада» убрана (вкладка стала «Перемещение», видна при >1 филиала); вся приёмка — только в «Закупках» (поставщик/цена/оплата/долг). Быстрое действие «Поступление» в карточке товара ведёт в «Закупки».

**Осталось по UX:** у долгов пока нет истории частичных погашений (только текущий остаток).

## ✅ Сделано (готово к работе)
- **Склад** доведён до продакшена: авторизация на products/stock, валидация DTO, транзакции, защита от перерасхода, мягко удалённые исключены из остатков.
- **Товары:** «Новый товар» — модальное окно, чистая форма (Название, Категория, Ед.изм, Продажная цена, Штрихкод+генерация, Фото + «Дополнительно»: мин.остаток, артикул). Форма редактирования — в том же виде. Закупочная цена — при приходе (не на карточке).
- **Генерация штрихкода** — внутренний EAN-13, уникальный (`GET /api/products/generate-barcode`).
- **Настройки → «Справочники»** — категории и единицы измерения.
- **Ценники** `/price-labels` — печать ценников со штрихкодом (jsbarcode), шаблоны размеров, печать листом.
- **PIN-кассир** — вход на кассе по PIN (токен 12ч, `POST /api/auth/pos-login`), управление PIN в «Сотрудниках».
- **Инфраструктура:** восстановлен модуль `UploadsModule`; `.gitignore` не прячет `src/uploads`.
- **Оборудование кассы:** сканер ШК (keyboard-wedge), дисплей покупателя VFD (Web Serial), **термопечать чеков ESC/POS** (Web Serial, CP866/латиница, авто-печать) — настройки в «Дисплей покупателя».
- **Касса+:** QR-чек (публичная страница `/r/:id`), профессиональный чек с реквизитами, алиасы штрихкодов (товар = неск. ШК).
- **Склад+:** импорт товаров из CSV/Excel, инвентаризация одним листом по филиалу.
- **Синхронизация:** раздел в настройках + кнопка «Синхронизировать сейчас» (server-side цикл), онлайн-фронт сам ходит в облачный API.

## ⏳ Что дальше (по приоритету)
1. **Деплой на Render** — проверить env (`DATABASE_URL`, `JWT_SECRET`) и прогон `prisma migrate deploy` при деплое (см. `docs/02-deploy-render.md`).
2. **Оборудование — остальное** (#6): термопринтер ESC/POS ✅, сканер ✅, VFD ✅. Осталось: **весы** (serialport), **резак/плоттер**, WebUSB-fallback для USB-принтеров без COM.
3. **Мультикасса / физические кассы** (#2): модель `Register` (device_id/api_key), локальная синхронизация касса↔сервер. Нужна, когда у клиента >1 кассы в магазине.
4. **Лицензирование Ed25519** (#3): для коробочной версии (привязка к MAC, `license.lic`, gate). Для облака не нужно.
5. **PIN «только локальная сеть»** — опция вкл/выкл проверки приватного IP (отложено, ломало бы облако).

## ⚠️ Важные решения/ловушки
- Это типография — **не** предлагать «магазинные» функции (весовой товар и т.п.). При неоднозначности — **сначала уточнять**.
- `.gitignore` содержит `**/uploads` — следит, чтобы не пряталось `src/uploads` (есть исключения).
- Владелец коммитит с разных машин — **всегда `git fetch`/`pull` перед работой**.
- Локальная разработка на Windows: перед `prisma generate`/`migrate` останавливать dev-сервер API (держит DLL движка Prisma).
